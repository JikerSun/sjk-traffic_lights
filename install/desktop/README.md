# 桌面悬浮 App 安装包

与 Cursor 扩展共用 `.ai-traffic-lights/state.json` 桥接文件。

| 目录 | 说明 |
|------|------|
| [mac/](mac/) | macOS `.dmg` / `.app`（预留） |
| [windows/](windows/) | Windows 安装包（预留） |

源码与开发：`products/desktop-overlay/`。构建流程确定后，产物将放入对应子目录。
