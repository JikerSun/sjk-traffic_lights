{
  "name": "ai-traffic-lights",
  "version": "0.1.0",
  "private": true,
  "description": "Shared core + Cursor extension + desktop overlay for AI status traffic lights.",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w @traffic-lights/protocol && npm run build -w @traffic-lights/core && npm run build -w @traffic-lights/adapters && npm run build -w ai-traffic-lights-cursor-extension && npm run build -w ai-traffic-lights-desktop-overlay",
    "test": "npm run test --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "debug:set:running": "node scripts/debug-bridge-state.mjs set RUNNING \"Manual debug: running\"",
    "debug:set:waiting": "node scripts/debug-bridge-state.mjs set WAITING_USER \"Manual debug: waiting user\"",
    "debug:set:done": "node scripts/debug-bridge-state.mjs set DONE \"Manual debug: done\"",
    "debug:set:idle": "node scripts/debug-bridge-state.mjs set IDLE \"Manual debug: idle\"",
    "debug:set:plan": "node scripts/debug-bridge-state.mjs set WAITING_PLAN_BUILD \"Manual debug: plan awaiting build\"",
    "debug:set:error": "node scripts/debug-bridge-state.mjs set ERROR \"Manual debug: error\"",
    "debug:next": "node scripts/debug-bridge-state.mjs next",
    "debug:reset": "node scripts/debug-bridge-state.mjs reset",
    "debug:sequence": "node scripts/debug-bridge-state.mjs run-sequence 2500",
    "bridge:watch": "node scripts/cursor-state-watcher.mjs",
    "bridge:watch:verbose": "node scripts/cursor-state-watcher.mjs --verbose",
    "bridge:watch:once": "node scripts/cursor-state-watcher.mjs --once",
    "verify:transcript": "node scripts/verify-transcript-state.mjs",
    "prepare:cursor-extension": "npm run build -w @traffic-lights/protocol && npm run build -w @traffic-lights/core && npm run build -w ai-traffic-lights-cursor-extension",
    "install:cursor-extension:local": "bash scripts/install-cursor-extension-local.sh",
    "bootstrap": "bash scripts/bootstrap-traffic-lights.sh",
    "package:extension": "npm run build -w ai-traffic-lights-cursor-extension && npm run package -w ai-traffic-lights-cursor-extension"
  },
  "engines": {
    "node": ">=20"
  }
}
