{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run AI Traffic Lights Extension",
      "type": "extensionHost",
      "request": "launch",
      "runtimeExecutable": "${execPath}",
      "args": [
        "--extensionDevelopmentPath=${workspaceFolder}/apps/cursor-extension"
      ],
      "preLaunchTask": "build-cursor-extension"
    }
  ]
}
