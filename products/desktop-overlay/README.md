# AI Traffic Lights — Desktop Overlay

**v0.1.2** — Mac Apple Silicon dmg · Multi Agent badge UI · Setup 向导 + Settings 菜单

- 与扩展 **v0.1.6** 同读 `state.json`（`displayMode` + `counts`）
- 首次启动自动安装 Cursor 全局 Hook（内置 hook-kit）

**用户安装：** [install/desktop/README.md](../../install/desktop/README.md) · Release **`desktop-v0.1.2`**

**开发：**

```bash
npm run dev:desktop
npm run build:desktop
```
